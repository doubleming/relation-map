import { IOption } from "../types/types";

export const defaultOption: IOption = {
    height: 500,
    width: 500,
    color: '#fff',
    nodeRadius: 30,
    borderColor: '#000',
    level: 3,
    startAngle: 30,
    radius: [180, 120, 80],
    defaultRadius: 40
}
